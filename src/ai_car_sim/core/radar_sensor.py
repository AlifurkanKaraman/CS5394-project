"""Radar distance-casting logic for the AI car simulation.

Encapsulates the ray-marching loop used to measure distances to track borders:
  - RadarSensorSystem: Class that owns the radar casting behaviour.
  - RadarSensorSystem.cast_radars(state, game_map, config): Cast rays at each configured
    angle from the car's current position, marching outward until a border pixel is hit or
    the maximum radar length is reached. Returns a list of SensorReading instances.
"""

pass
