"""2-D vector mathematics utilities for the AI car simulation.

Provides pure functions with no side effects:
  - distance(a, b): Euclidean distance between two 2-D points.
  - rotate_point(point, origin, angle): Rotate a point around an origin by a given angle.
  - clamp(value, min_val, max_val): Clamp a numeric value within [min_val, max_val].
  - heading_to_vector(angle_degrees): Convert a heading angle to a unit direction vector.
"""

pass
