"""
Per-run metrics collection and aggregation.

GenerationMetrics is a dataclass capturing generation number, alive count,
best fitness, average fitness, elapsed seconds, and track name.
RunMetricsCollector provides record_generation, record_run_summary, to_dict,
and to_rows methods for accumulating and exporting metrics across a training run.
"""

pass
