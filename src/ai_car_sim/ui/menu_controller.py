"""
Menu navigation and pre-simulation user input.

MenuController manages track selection and mode selection (train / replay) via
a pygame event loop that runs before the simulation starts. Returns the user's
chosen track and mode to the caller.
"""

pass
