"""
Abstract driver contract for the AI layer.

Defines the Action enum with values TURN_LEFT, TURN_RIGHT, SLOW_DOWN, SPEED_UP,
and the DriverInterface abstract base class with an abstract decide_action method
that takes sensor inputs and vehicle state and returns an Action.
"""

pass
