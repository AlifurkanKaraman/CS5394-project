"""
NEAT-based driver implementation.

NeatDriver wraps a neat.nn.FeedForwardNetwork and implements the DriverInterface.
decide_action activates the network with the current sensor inputs and selects
the output action via argmax over the network's output vector.
"""

pass
