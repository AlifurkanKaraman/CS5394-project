"""
Single radar sensor reading data model.

Holds the endpoint coordinates (x, y) where the radar ray terminated
and the integer distance from the car's centre to that endpoint.
Produced by the radar sensor system and consumed by the AI driver.
"""

pass
