"""
Simulation configuration parameters data model.

Holds all tunable settings for a simulation run: car sprite dimensions
(car_size_x, car_size_y), border color for collision detection,
radar settings (max_radar_length, radar_angles), maximum car speed,
screen dimensions (screen_width, screen_height), target frames-per-second,
and the generation timeout expressed in frames.
"""

pass
